<h1><%= @cocktail.name %></h1>
<%= @cocktail.doses.each do |f| %>
  <%= f.description %>
  <%= f.ingredient.name %>
<% end %>

<h2><%= link_to "delete me", cocktail_path(@cocktail), method: :delete%></h2>
<h2><%= link_to "add recipe description", new_cocktail_dose_path(@cocktail) %></h2>
<h2><%= link_to "delete recipe", dose_path(@cocktail), method: :delete%></h2>

